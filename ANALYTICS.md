# 방문 기록 측정 (Google Analytics 4)

사이트의 모든 화면과 주요 버튼에 이벤트 로그가 심어져 있습니다.
`NEXT_PUBLIC_GA_ID` 환경변수를 넣는 순간 이 기록들이 한꺼번에 살아나고,
넣지 않으면 GA 스크립트 자체를 불러오지 않습니다 (쿠키도 생기지 않습니다).

---

## 1. 연결하는 방법

1. **https://analytics.google.com** 접속 → 구글 계정으로 로그인
2. 왼쪽 아래 **관리(⚙)** → **만들기 → 속성**
   - 속성 이름: `Dr. GL`
   - 보고 시간대: `대한민국`, 통화: `대한민국 원(₩)`
3. 업종/규모는 아무거나 골라도 지표에 영향 없음 → **만들기**
4. 데이터 스트림 선택 화면에서 **웹** 선택
   - 웹사이트 URL: `https://dr-gl.vercel.app`
   - 스트림 이름: `Dr. GL 웹`
5. 만들고 나면 우측 상단에 **측정 ID**가 나옵니다 → `G-` 로 시작하는 값 복사
6. **Vercel** → 프로젝트 → **Settings → Environment Variables**
   - Key: `NEXT_PUBLIC_GA_ID`
   - Value: 복사한 `G-XXXXXXXXXX`
   - Environment: Production (Preview 도 켜두면 테스트 배포까지 잡힙니다)
7. **Deployments → 맨 위 배포의 ⋯ → Redeploy**
   `NEXT_PUBLIC_` 값은 빌드 시점에 코드에 박히므로 **반드시 재배포해야** 적용됩니다.
8. 사이트를 열어본 뒤 GA의 **보고서 → 실시간**에서 본인 접속이 잡히면 연결 완료.

> ⚠️ **먼저 할 일:** GA를 켜면 방문자에게 분석 쿠키가 생깁니다.
> 지금 발행된 개인정보처리방침에는 "분석 쿠키를 사용하지 않는다"고 적혀 있으니,
> 아래 4번의 문구로 **새 버전을 먼저 발행한 뒤에** 환경변수를 넣어주세요.

---

## 2. 남는 기록

| 이벤트 이름 | 언제 남는가 | 같이 남는 값 |
| --- | --- | --- |
| `page_view` | 화면이 바뀔 때마다 (홈 → 상세도 포함) | 경로, 제목 |
| `page_engagement` | 그 화면을 떠날 때 — **머문 시간** | 경로, 초 |
| `scroll_depth` | 25 / 50 / 75 / 100% 까지 읽었을 때 | 경로, 퍼센트 |
| `select_content` | 콘텐츠 카드·배너를 눌러 상세로 들어갈 때 | 작품 id·제목·카테고리·착즙 여부, **어느 목록의 몇 번째 카드였는지** |
| `outbound_platform_click` | 상세의 "시청·감상 가능한 곳"을 눌러 밖으로 나갈 때 — **가장 중요한 지표** | 작품, 플랫폼 이름·유료/무료, 도착 도메인 |
| `search` | 검색 결과가 그려졌을 때 | 검색어, **결과 건수** (0건 검색어 = 채워야 할 콘텐츠) |
| `search_suggestion_click` | 추천 태그 칩 클릭 | 태그 |
| `filter_change` | 카테고리/국가/착즙 필터 조작 | 필터 종류, 고른 값 |
| `nav_click` | 헤더·푸터·뒤로가기 등 이동 버튼 | 버튼 이름, 어디서 어디로 |
| `submission_start` | 제보 폼을 연 순간 | — |
| `submission_complete` | 제보 제출 성공 | 카테고리, 국가, 착즙, 플랫폼/이메일 입력 여부 |
| `submission_error` | 제보 실패 | 실패 사유 |
| `comment_submit` | 한줄평 등록 성공 | 작품 id, 글자 수 |
| `comment_expand` | 한줄평 더보기 | 작품 id, 총 개수 |
| `board_notify_signup` | 게시판 오픈 알림 신청 | — |

검색어 외에 이용자가 입력한 내용(한줄평 본문, 이메일, 제보 링크)은 보내지 않습니다.
외부 링크도 전체 주소가 아니라 **도메인만** 보냅니다.

## 3. GA에서 봐야 할 곳

- **실시간** — 지금 몇 명이 어디를 보고 있는지
- **보고서 → 참여도 → 페이지 및 화면** — 화면별 방문수와 평균 참여 시간
- **보고서 → 참여도 → 이벤트** — 위 표의 이벤트별 발생 횟수
- **탐색(Explore) → 자유 형식** — 예: `select_content` 를 `list_name` 별로 쪼개면
  "홈의 어느 줄이 실제로 눌리는가"가 나옵니다. 홈 큐레이션 순서를 정하는 근거가 됩니다.
- **맞춤 측정기준 등록** (관리 → 데이터 표시 → 맞춤 정의):
  `content_title`, `list_name`, `platform_name`, `search_term`, `label` 을 등록해두면
  보고서에서 이 값들로 쪼개볼 수 있습니다. **등록한 이후에 쌓인 데이터부터** 보입니다.

## 4. GA를 켜기 전에 발행할 개인정보처리방침 문구

`/admin` → **법적 고지** → 개인정보처리방침 → 아래 문단으로 교체 →
시행일과 변경 요약("방문자 분석 도구(GA) 사용 반영")을 적고 **새 버전으로 저장하고 발행**.

바꿀 곳은 "## 2. 자동으로 수집되는 정보" 의 두 번째 문단입니다.

```
Dr. GL은 서비스 개선을 위해 구글의 방문자 분석 도구인 Google Analytics를 사용합니다. 이 과정에서 방문한 화면, 머문 시간, 누른 버튼, 검색어, 기기·브라우저 종류, 대략적인 지역 정보가 쿠키를 통해 수집되어 Google LLC(미국)로 전송·보관됩니다. 이름·이메일처럼 개인을 직접 식별할 수 있는 정보는 전송하지 않습니다.

브라우저 설정에서 쿠키를 차단하거나, 구글이 제공하는 [Google Analytics 차단 브라우저 부가기능](https://tools.google.com/dlpage/gaoptout)을 설치하면 수집을 거부할 수 있습니다. 이 경우에도 서비스 이용에는 제한이 없습니다.

어드민 로그인 상태를 유지하기 위한 쿠키는 운영자에게만 발급됩니다.
```

"## 4. 처리 위탁 및 국외 이전" 목록에도 한 줄 추가해주세요.

```
- **Google LLC** — 방문자 분석 (미국)
```

## 5. 나중에 애드센스를 달 때

- 광고 심사는 **개인정보처리방침 페이지**와 **문의 수단**을 봅니다. 둘 다 이미 있습니다.
- 승인 후에는 개인정보처리방침에 광고 쿠키(Google AdSense) 문단을 한 번 더 추가해야 합니다.
- 유럽(EEA·영국) 방문자가 있다면 구글이 **동의 관리 도구(CMP)** 를 요구합니다.
  필요해지면 그때 붙이면 됩니다 — 지금 코드는 GA를 끄고 켜는 것만으로 동작합니다.
